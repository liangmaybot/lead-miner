export class ApifyClient {
  constructor(options = {}) {
    this.token = options.token || null;
  }

  actor() {
    return {
      call: async () => {
        throw new Error('ApifyClient stub: network disabled. Install dependencies and provide APIFY_TOKEN.');
      },
    };
  }

  dataset() {
    return {
      listItems: async () => ({ items: [] }),
    };
  }
}
